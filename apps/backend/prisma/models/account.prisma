model Account {
  id       String @id
  username String @unique
  password String

  roleId String @map("role_id")
  role   Role   @relation(fields: [roleId], references: [id])

  devices Device[]

  @@map("accounts")
}
